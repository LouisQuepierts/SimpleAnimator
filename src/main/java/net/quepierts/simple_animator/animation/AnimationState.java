package net.quepierts.simple_animator.animation;

import net.quepierts.simple_animator.client.state.*;

public enum AnimationState {
    IDLE,
    REQUEST,
    WAITING,
    ENTER,
    LOOP,
    EXIT;
}
